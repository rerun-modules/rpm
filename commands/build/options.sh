# Generated by stubbs:add-option. Do not edit, if using stubbs.
# Created: Sun Oct 21 09:41:35 PDT 2012
#
#/ usage: rpm:build [ --arch|-a <noarch>]  --name|-n <>  --release|-r <> [ --topdir|-t </usr/src/rpm>]  --version|-v <> 

# print USAGE and exit
rerun_option_usage() {
    grep '^#/ usage:' <"$0" | cut -c4- >&2
    return 2
}

# print SYNTAX and exit
rerun_option_error() {
    if [[ "$RERUN_COLOR" == "true" ]]
    then echo >&2 -e """SYNTAX: $*"""
    else echo >&2 "SYNTAX: $*" 
    fi
    exit 2
}

# check option has its argument
rerun_option_check() {
    [ "$1" -lt 2 ] && rerun_option_usage
}

# options: [arch name release topdir version]
while [ "$#" -gt 0 ]; do
    OPT="$1"
    case "$OPT" in
        --arch|-a) rerun_option_check $# ; ARCH=$2 ; shift ;;
        --name|-n) rerun_option_check $# ; NAME=$2 ; shift ;;
        --release|-r) rerun_option_check $# ; RELEASE=$2 ; shift ;;
        --topdir|-t) rerun_option_check $# ; TOPDIR=$2 ; shift ;;
        --version|-v) rerun_option_check $# ; VERSION=$2 ; shift ;;
        # help option
        -?)
            rerun_option_usage
            exit 2
            ;;
        # end of options, just arguments left
        *)
          break
    esac
    shift
done

# If defaultable options variables are unset, set them to their DEFAULT
[ -z "$ARCH" ] && ARCH="noarch"
[ -z "$TOPDIR" ] && TOPDIR="/usr/src/rpm"
# Check required options are set
[ -z "$NAME" ] && { echo >&2 "missing required option: --name" ; return 2 ; }
[ -z "$RELEASE" ] && { echo >&2 "missing required option: --release" ; return 2 ; }
[ -z "$VERSION" ] && { echo >&2 "missing required option: --version" ; return 2 ; }
# If option variables are declared exportable, export them.

#
return 0
